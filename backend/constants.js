const X_API = 'x-api';

const LOG_SOURCE = {
    REQUEST: 'Request',
    RESPONSE: 'Response',
    AUTH_CHECK: 'Auth Check',
};

const LOG_PATH = './AppData/log/log_.txt';
const TOKEN_PATH = './AppData/tokens';

module.exports = {
    X_API,
    LOG_SOURCE,
    LOG_PATH,
    TOKEN_PATH,
};